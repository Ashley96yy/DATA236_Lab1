# Lab 2 Service Layout

This folder contains the service-level backend structure for Lab 2.

Planned services:

- `user-service`
- `owner-service`
- `restaurant-service`
- `review-api-service`
- `review-worker-service`

The existing Lab 1 backend under `backend/app/` remains intact during Phase 0 and acts as the source implementation while the Lab 2 service split is prepared.
