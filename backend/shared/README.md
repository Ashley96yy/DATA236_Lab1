# Lab 2 Shared Backend Modules

This folder contains shared infrastructure for the Lab 2 service split.

Planned shared modules:

- `config` for settings and environment loading
- `auth` for JWT and bcrypt helpers
- `db` for MongoDB connections and repositories
- `kafka` for producer and consumer helpers
- `schemas` for shared request and response models
- `utils` for small shared helpers
