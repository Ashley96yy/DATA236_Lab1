apiVersion: v1
kind: Service
metadata:
  name: review-api-service
  namespace: yelp-lab2
spec:
  selector:
    app: review-api-service
  ports:
    - port: 8004
      targetPort: 8004
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-api-service
  namespace: yelp-lab2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: review-api-service
  template:
    metadata:
      labels:
        app: review-api-service
    spec:
      containers:
        - name: review-api-service
          image: lab1-review-api-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8004
          envFrom:
            - configMapRef:
                name: lab2-config
            - secretRef:
                name: lab2-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 10
            periodSeconds: 10
